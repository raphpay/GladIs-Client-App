export default interface IModule {
  id: string;
  name: string;
  index: number;
}

export interface ICheckBoxOption {
  id: string;
  name: string;
  value: boolean;
}

export interface IModuleInput {
  name: string;
  index: number;
}
export default interface IModule {
  id: string;
  name: string;
}

export interface ISubCategory {
  id: string;
  title: string;
}

export interface IDocumentInput {
  name: string,
  path: string
}
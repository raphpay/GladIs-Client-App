enum PlatformName {
  Mac = 'macos',
  Android = 'android',
  IOS = 'ios',
  Windows = 'windows',
}

export enum Orientation {
  Portrait = 'portrait',
  Landscape = 'landscape',
}

export default PlatformName;